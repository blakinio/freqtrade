name: AI Platform Phase 5 Final Holdout Validation (retired v1)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired-final-holdout-v1:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retired consumed holdout workflow
        run: |
          echo "Phase 5 final holdout v1 is retired."
          echo "The 20260501-20260630 holdout has already been consumed and must never be reused as unseen final-validation data."
          echo "No market data is downloaded and no validation is executed by this workflow."
