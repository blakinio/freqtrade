name: AI Platform RL-v2 Seed Format Cleanup

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  inert:
    name: Inert cleanup placeholder
    runs-on: ubuntu-24.04
    steps:
      - name: No operation
        run: echo "Temporary workflow is inert and pending deletion."
