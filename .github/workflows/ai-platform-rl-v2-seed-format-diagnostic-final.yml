name: AI Platform RL-v2 Seed Format Diagnostic Final

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary formatter completed; workflow pending deletion."
