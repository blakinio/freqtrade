name: CI optimizer bootstrap disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Bootstrap disabled; target files are applied through the GitHub contents API."
