name: Inspect Portal Proof Run

on:
  pull_request:
    paths:
      - .github/workflows/diagnose-liquid20-deploy-run.yml

permissions:
  actions: read
  contents: read

jobs:
  inspect:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Inspect exact portal proof push run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          python - <<'PY'
          import json
          import os
          import urllib.request

          sha = "9ff7717cde0127c12a9cb9da576599f4bbdf6954"
          repository = os.environ["GITHUB_REPOSITORY"]
          request = urllib.request.Request(
              f"https://api.github.com/repos/{repository}/actions/runs?head_sha={sha}&event=push&per_page=100",
              headers={
                  "Accept": "application/vnd.github+json",
                  "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
                  "X-GitHub-Api-Version": "2022-11-28",
              },
          )
          with urllib.request.urlopen(request, timeout=30) as response:
              payload = json.load(response)
          runs = [
              {
                  "id": item["id"],
                  "name": item["name"],
                  "status": item["status"],
                  "conclusion": item["conclusion"],
                  "head_sha": item["head_sha"],
                  "run_attempt": item["run_attempt"],
                  "created_at": item["created_at"],
                  "updated_at": item["updated_at"],
              }
              for item in payload.get("workflow_runs", [])
              if item.get("name") == "Liquidations Live Portal Synology Proof"
          ]
          print("PORTAL_PROOF_RUNS=" + json.dumps(runs, sort_keys=True))
          if not runs:
              raise SystemExit("No exact portal proof push run found")
          PY
