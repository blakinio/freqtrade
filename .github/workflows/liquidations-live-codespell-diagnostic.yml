name: Liquidations Live Codespell Diagnostic

on:
  pull_request:
    branches:
      - develop
    paths:
      - .github/workflows/liquidations-live-codespell-diagnostic.yml

permissions: {}

jobs:
  diagnose:
    name: Capture codespell output
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout exact source
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6.2.0
        with:
          python-version: "3.12"
      - name: Capture codespell
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check codespell==2.4.2 tomli
          set +e
          codespell ai_platform tests/ai_platform_integration/test_liquidation_live_stream.py tests/ai_platform_integration/test_synology_liquid20_live_deployment.py > "$RUNNER_TEMP/codespell.log" 2>&1
          printf '%s\n' "$?" > "$RUNNER_TEMP/codespell.status"
          exit 0
      - name: Upload diagnostic
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: liquidations-live-codespell-${{ github.event.pull_request.number }}
          path: |
            ${{ runner.temp }}/codespell.log
            ${{ runner.temp }}/codespell.status
          if-no-files-found: error
          retention-days: 1
