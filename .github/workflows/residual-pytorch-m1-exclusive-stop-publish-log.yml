name: Residual PyTorch M1 Exclusive Stop Publish Log

on:
  pull_request:
    types: [opened]
    branches:
      - develop
    paths:
      - ".github/workflows/residual-pytorch-m1-exclusive-stop-publish-log.yml"

permissions:
  actions: read
  contents: read

jobs:
  collect:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Download exact publish failure log
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        run: |
          set -euo pipefail
          mkdir -p failure-log
          curl --fail-with-body --location \
            --header "Accept: application/vnd.github+json" \
            --header "Authorization: Bearer ${GH_TOKEN}" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/${REPOSITORY}/actions/jobs/89849681205/logs" \
            --output failure-log/publish.log
          tail -n 220 failure-log/publish.log > failure-log/publish-tail.log
          grep -E "fatal:|error:|failed|rejected|remote:|git rm|git push|nothing to commit|denied|permission" \
            failure-log/publish.log > failure-log/failure-lines.txt || true
      - name: Upload exact publish failure evidence
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: residual-pytorch-m1-exclusive-stop-publish-log
          path: failure-log/
          if-no-files-found: error
          retention-days: 1
