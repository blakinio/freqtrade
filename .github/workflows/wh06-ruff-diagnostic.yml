name: WH-06 Ruff diagnostic

on:
  pull_request:
    paths:
      - "ai_platform/wickhunter/**"
      - "tests/ai_platform_integration/test_wickhunter_portal_risk.py"
      - ".github/workflows/wh06-ruff-diagnostic.yml"

permissions:
  contents: read

jobs:
  diagnostic:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false

      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: "3.12"

      - name: Capture exact Ruff fix diff
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --disable-pip-version-check ruff==0.15.21
          ruff check --fix ai_platform/wickhunter tests/ai_platform_integration/test_wickhunter_portal_risk.py
          git diff -- ai_platform/wickhunter tests/ai_platform_integration/test_wickhunter_portal_risk.py > wh06-ruff-fix.diff
          ruff check ai_platform/wickhunter tests/ai_platform_integration/test_wickhunter_portal_risk.py > wh06-ruff-after.txt 2>&1

      - name: Upload Ruff fix diff
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: wh06-ruff-fix-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            wh06-ruff-fix.diff
            wh06-ruff-after.txt
          if-no-files-found: error
          retention-days: 1
