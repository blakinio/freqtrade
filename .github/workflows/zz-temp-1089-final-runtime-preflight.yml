name: Temporary 1089 post-build runtime recovery probe

on:
  push:
    branches:
      - diag/1089-final-runtime-preflight-20260809

permissions:
  contents: read

jobs:
  probe:
    runs-on: [freqtrade-staging]
    timeout-minutes: 5
    steps:
      - name: Refuse trading credential environment
        shell: bash
        run: |
          set -euo pipefail
          for name in \
            OKX_API_KEY OKX_API_SECRET OKX_SECRET_KEY OKX_PASSPHRASE \
            BYBIT_API_KEY BYBIT_API_SECRET BINANCE_API_KEY BINANCE_API_SECRET \
            FT_EXCHANGE_KEY FT_EXCHANGE_SECRET \
            FREQTRADE__EXCHANGE__KEY FREQTRADE__EXCHANGE__SECRET
          do
            test -z "${!name:-}" || exit 1
          done

      - name: Checkout merged implementation
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0
        with:
          persist-credentials: false
          ref: 27b6b65ad6a79184c4922a79a86f6e72b3d4383e

      - name: Probe Docker runtime after protected image build failure
        shell: bash
        run: |
          set -euo pipefail
          timeout 10 docker info --format 'before_running={{.ContainersRunning}} stopped={{.ContainersStopped}} live_restore={{.LiveRestoreEnabled}}'
          python3 deploy/synology/portal-oidc/docker_runtime_preflight.py
          timeout 10 docker info --format 'after_running={{.ContainersRunning}} stopped={{.ContainersStopped}} live_restore={{.LiveRestoreEnabled}}'
