# Temporary hardened candidate runtime binding trigger

Run the bounded self-deleting implementation and focused validation on `feat/wickhunter-candidate-runtime-binding-v1`, including rollback identity binding and the pytest collection repair. Close without merge after the feature branch is updated.
