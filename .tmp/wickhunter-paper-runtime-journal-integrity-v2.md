# Temporary structural paper runtime journal integrity trigger

Bind every generation to the previous manifest, runtime policy and canonical observation identity; add substitution regressions; run format, Ruff, mypy and focused tests; remove both temporary integrity workflows. Close without merge after the feature branch updates.
