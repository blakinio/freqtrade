# Temporary commit-scoped paper runtime journal integrity trigger

Reuse the complete integrity hardening, replace only the ambiguous generation verification inside `commit`, run formatting, Ruff, mypy and focused tests, then remove all four temporary integrity workflows. Close without merge after the feature branch updates.
