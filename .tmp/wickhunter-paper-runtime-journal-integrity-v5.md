# Temporary label-scoped paper runtime journal integrity trigger

Reuse the complete integrity hardening, locate the ambiguous operation by its unique semantic label, scope the replacement to `commit`, run formatting, Ruff, mypy and focused tests, then remove all five temporary integrity workflows. Close without merge after the feature branch updates.
