# Temporary direct paper runtime journal integrity trigger

Apply the integrity hardening directly to the current product source by stable method boundaries, add generation substitution regressions, run formatting, Ruff, mypy and focused tests, then remove all temporary integrity workflows. Close without merge after the feature branch updates.
