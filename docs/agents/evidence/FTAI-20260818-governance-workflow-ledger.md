# FTAI-20260818 governance workflow ledger

Authority snapshot inspected: `develop@782f0c8cdb5f24e83a2bc9ad9660df1474a470ab`.

Purpose: classify workflows whose names contain legacy `shadow`, `paper`, `staging`, `production` or `live` semantics from their actual triggers/operations. Classification is evidence-based; names alone are not execution or capital authority.

| Workflow | Trigger / direct dependency evidence | Current risk semantics | Classification | Implementation in #1595 |
| --- | --- | --- | --- | --- |
| `.github/workflows/ai-platform-binance-spot-instrument-shadow-acceptance.yml` | PR `opened` to `develop` only when exact frozen run-request is added; runs 24h on `freqtrade-staging` / `synology-staging`; public Binance data; request requires `execution_enabled=false`, no credentials/proxy | `persistent_data`, `shared_synology_mutation`; no capital authority | KEEP | Keep unchanged; frozen one-shot contract and durable evidence path still encode safety. Rename would require coordinated request/artifact migration. |
| `.github/workflows/ai-platform-binance-spot-instrument-shadow-acceptance-v3.yml` | PR `opened` initializes exact request; `*/5` schedule samples durable state on `freqtrade-staging`; credential/proxy environment is refused | `persistent_data`, `shared_synology_mutation`; public-data collection | KEEP | Keep unchanged; active scheduler semantics make filename-only cleanup unsafe. |
| `.github/workflows/ai-platform-okx-liquidation-shadow-acceptance.yml` | PR `opened/reopened/synchronize` for exact request; 24h Synology acceptance; recognized trading credentials refused | `persistent_data`, `shared_synology_mutation`; public-data acceptance | KEEP | Keep unchanged. |
| `.github/workflows/ai-platform-okx-liquidation-shadow-acceptance-staging-preflight.yml` | PR path-limited exact preflight; verifies Synology runner/storage and public OKX endpoints; creates/removes bounded durable-root probe | `shared_synology_mutation` and storage preflight; no trading | KEEP | Keep unchanged as safety preflight. |
| `.github/workflows/ai-platform-okx-liquidation-shadow-smoke.yml` | PR `opened` for exact request; GitHub-hosted Ubuntu; public OKX smoke; recognized trading credentials refused | low-risk public transport smoke; no shared state | KEEP | Keep unchanged; this is evidence that `shadow` is not a capital-risk synonym. |
| `.github/workflows/liquidations-live-synology.yml` | push to `develop` for Liquid20/deploy paths plus `workflow_dispatch`; deploys exact-SHA continuous public liquidation collector on `freqtrade-staging` | `deployment`, `shared_synology_mutation`, `persistent_data`; `live` describes public stream, not trading | KEEP | Keep unchanged; deployment remains risk-selected and needs target evidence. |
| `.github/workflows/liquidations-live-operational-health.yml` | push/path, `*/5` schedule, manual dispatch; GitHub control-plane status plus Synology collector/portal health | operational health/read path; issue/status mutation; no order authority | KEEP | Keep unchanged; it is ongoing health evidence for deployed public-data services. |
| `.github/workflows/liquidations-live-portal-synology-proof.yml` | push/path plus manual dispatch; `freqtrade-staging`; explicitly proves read-only portal path and publishes status/artifacts | target/user-path proof against shared staging | KEEP | Keep unchanged; use when applicable rather than as universal E2E. |
| `.github/workflows/liquidations-live-telegram-notifications.yml` | issues, `*/5` schedule, push/path and bounded manual test; GitHub-hosted; consumes Telegram bot token/chat-id secrets | `auth_or_secrets`, external notification workflow | KEEP | Keep unchanged; secret-boundary and real-channel testing remain targeted gates. |
| `.github/workflows/portal-staging-external-e2e.yml` | manual dispatch only; external Cloudflare staging probe; uses staging access/probe secrets | `auth_or_secrets`, `user_workflow_change` evidence provider | KEEP | Keep manual/targeted; do not make universal. |
| `.github/workflows/portal-staging-policy.yml` | PR path-limited to Cloudflare staging policy/tests/docs/workflows; GitHub-hosted policy/unit tests | focused deployment-policy validation, no runtime mutation | KEEP | Keep as focused path gate. |
| `.github/workflows/ai-platform-wickhunter-production-market-evidence.yml` | exact request PR plus scheduler; persistent Docker collector on Synology; immutable request; credential/proxy refusal | `deployment`, `persistent_data`, `shared_synology_mutation`, `research_integrity`; no trading authority | KEEP | Keep unchanged; `production` means persistent research evidence here. |
| `.github/workflows/ai-platform-wickhunter-production-market-evidence-v2.yml` | exact request PR; persistent OKX evidence collector on Synology; credential/proxy refusal | `deployment`, `persistent_data`, `shared_synology_mutation`, `research_integrity`; no trading authority | KEEP | Keep unchanged; v2 has a distinct durable root/contract and cannot be merged by name alone. |
| `.github/workflows/ai-platform-wickhunter-wh09-production-research-runtime-deploy.yml` | push to `develop` only for exact immutable retry request; deploys exact frozen WH09 research runtime on Synology; contract states `mode=shadow`, no credentials/order adapter/execution/live capital | `deployment`, `persistent_data`, `shared_synology_mutation`, `research_integrity`, model identity; no capital authority | KEEP | Keep dormant exact-request executor; no evidence in #1595 justifies deleting its historical deployment contract. |
| `.github/workflows/wickhunter-1396-paper-proof-trusted.yml` | PR path-limited exact proof request; Synology; builds exact image, creates bounded PAPER activation and runs natural cycles; request and runtime enforce zero trading/live-capital authority | `model_activation`, `research_integrity`, `shared_synology_mutation`, real runtime proof without capital | KEEP | Keep unchanged; `PAPER` is a historical runtime-mode term but the proof still encodes useful zero-authority constraints. |
| `.github/workflows/wickhunter-production-live-archive-conversion.yml` | PR path-limited exact conversion request; Synology; reads closed Liquid20 archive and creates no-overwrite accepted import; all execution/trading/model/live-capital flags must be false | `persistent_data`, `shared_synology_mutation`, `research_integrity`; bounded write, no capital authority | KEEP | Keep unchanged; conversion is research-data processing, not live trading. |
| `.github/workflows/wickhunter-wh09-live-contract-repair.yml` | push to `develop` when its workflow/payload changes; grants `contents: write`; merges `develop` into and pushes `feat/wickhunter-wh09-paper-runtime-operator-20260803-v1`; posts to Issue #1144. CI registry also tracks this exact workflow and `tools/ci/workflow_catalog.py` is the canonical registry/catalog lifecycle generator. | historical repository self-repair with write authority; target branch is absent and Issue #1144 is closed | RETIRE | Do not delete in #1595. An attempted file-only removal was rejected by `tests/ci/test_workflow_validation.py` because `.github/workflow-registry.yaml` remained authoritative. Restore the exact file and defer retirement to a bounded lifecycle task that regenerates registry/catalog and safely handles the historical Actions workflow record. |

## Dependency conclusion

None of the inspected legacy-named workflows above uses `workflow_call`; their direct callers are event/path/request contracts shown above. The active `shadow/paper/staging/production/live` terms therefore represent several distinct domains: public data, research runtime, shared-host deployment, external E2E, notifications and historical one-shot repair.

No legacy-named workflow is physically removed, renamed or merged in #1595. The WH09 self-repair workflow is the only `RETIRE` candidate with strong obsolescence evidence, but CI exposed a current registry/catalog lifecycle dependency; retirement is therefore intentionally deferred rather than performed as an incomplete file-only deletion.
